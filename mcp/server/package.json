{
  "name": "ainimation-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP de ainimation.studio (Streamable HTTP): las Xperiencias del estudio —listar, leer, publicar— como herramientas para los agentes de la flota AdmiraNeXT.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check src/index.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}
